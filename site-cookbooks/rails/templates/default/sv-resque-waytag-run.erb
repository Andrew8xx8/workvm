#!/usr/bin/env bash

exec 2>&1

/usr/bin/env HOME=/u/apps/<%= @options[:app] %>/current RACK_ENV=<%= @options[:env] %> chpst -u <%= @options[:user] %>:<%= @options[:group] %> /usr/local/rvm/bin/rvm-shell -l -c 'cd /u/apps/<%= @options[:app] %>/current; bundle exec rake resque:workers RAILS_ENV=<%= @options[:env] %> QUEUE=* COUNT=<%= @options[:count] %> PIDFILE=tmp/pids/resque_worker_QUEUE.pid'
